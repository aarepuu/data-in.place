version: '3.3'

networks:
  web:
    external: true

services:
  app:
    container_name: ${APP}-datainplace
    platform: linux/amd64
    build: .
    restart: always
    expose:
      - 80
    networks:
      - web
    labels:
      - 'traefik.enable=true'
      - 'traefik.docker.network=web'
      - 'traefik.http.routers.${APP}-datainplace.rule=Host(`${APP}.${DEPLOYMENT}`)'
      - 'traefik.http.routers.${APP}-datainplace.entrypoints=websecure'
      - 'traefik.http.routers.${APP}-datainplace.tls=true'
      - 'traefik.http.routers.${APP}-datainplace.tls.certresolver=myresolver'
    env_file: .env
    logging:
      driver: 'json-file'
      options:
        max-size: '1024k'
        max-file: '10'
